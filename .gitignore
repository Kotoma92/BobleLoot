# Local secrets — never commit.
.env
.env.local
.env.*.local

# Python tool cache.
__pycache__/
*.pyc

# Generated guild data — never commit (contains roster info).
Data/BobleLoot_Data.lua

BobleLoot-*.zip

# Brainstorming companion mockups (local design scratch space).
.superpowers/

# Claude Code local state (agent worktrees, tasks, etc).
.claude/
