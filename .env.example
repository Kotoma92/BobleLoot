# Copy this file to `.env` and fill in your real values.
# `.env` is gitignored so secrets stay on your machine.

# WoWAudit team API key (Settings -> API on https://wowaudit.com).
WOWAUDIT_API_KEY=your-team-api-key-here
